### Running the Service

To run the service locally, use the following command:

```bash
$pip install poetry
$poetry shell
$poetry install
$(cd .app/ && poetry run python manage.py runserver)
```
